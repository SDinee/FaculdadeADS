CREATE TABLE inscricao_evento_a (
  id         SERIAL PRIMARY KEY,
  aluno_nome VARCHAR(80),
  curso      VARCHAR(50)
);

CREATE TABLE inscricao_evento_b (
  id         SERIAL PRIMARY KEY,
  aluno_nome VARCHAR(80),
  curso      VARCHAR(50)
);
